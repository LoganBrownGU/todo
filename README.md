# todo
A very simple to-do list

## Usage
`./ install.sh    # will prompt for sudo`

`todo         # to display todo list`

`todo edit    # to edit todo list`

`todo config` # to edit configuration

## Other info

The todo file is `$HOME/.todo`

The config file is `$HOME/.config/todo/config`
